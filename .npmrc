; engines.node in package.json is advisory by default — npm only warns.
; Node 24 is the pinned line (.nvmrc); fail the install instead of surfacing
; the mismatch later as an unrelated runtime error.
engine-strict=true
