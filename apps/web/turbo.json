{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "build:standalone": {
      "env": ["NEXT_PUBLIC_MODE"],
      "outputs": ["out/**"]
    }
  }
}
