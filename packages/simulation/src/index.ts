// Placeholder export — the rules engine and simulation loop land in Epic 3.
// Story 1.3 replaced @gol/domain's placeholder export with real schemas; this stub no longer
// cross-imports domain until Epic 3 actually consumes the generic rules-engine types.
export const GOL_SIMULATION = '@gol/simulation' as const;
